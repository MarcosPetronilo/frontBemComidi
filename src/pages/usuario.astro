---
const title = 'BemComido - Usuário';
---
<html lang="pt-BR">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>{title}</title>
    <link rel="stylesheet" href="/src/styles/global.css" />
  </head>
  <body class="min-h-screen">
    <header class="w-full bg-brandRed text-white shadow-md">
      <div class="max-w-6xl mx-auto px-4 py-3 flex items-center gap-3">
        <a href="/app" class="px-3 py-1 bg-[#b21f1f] rounded shadow-button">Home</a>
        <div class="mx-auto flex items-center gap-2 text-2xl font-logo">
          <span>BC</span>
          <span aria-hidden="true">☕</span>
        </div>
        <div class="flex items-center gap-2 ml-2">
          <span>Bem vindo, Usuário</span>
          <span class="inline-block h-7 w-7 rounded-full bg-white text-brandRed grid place-items-center">👤</span>
        </div>
      </div>
    </header>

    <main class="max-w-md mx-auto px-4 py-10">
      <div class="bg-[#f9e3ac] rounded-md p-6 shadow-md ring-1 ring-black/10 space-y-4">
        <h1 class="text-2xl text-brandBrown mb-4">Menu do Usuário</h1>
        <a id="editarInfo" href="#" class="block text-center bg-brandRed text-white py-2 rounded-md shadow-button">Editar Informações</a>
        <a id="minhasReceitas" href="#" class="block text-center bg-brandRed text-white py-2 rounded-md shadow-button">Minhas Receitas</a>
        <button id="sair" class="w-full bg-brandRed text-white py-2 rounded-md shadow-button">Sair</button>
        <p id="userMsg" class="mt-2 text-center text-sm"></p>
      </div>
    </main>
  </body>
</html>
<script>
  const sairBtn = document.getElementById('sair');
  const msg = document.getElementById('userMsg');
  const editarLink = document.getElementById('editarInfo');
  const minhasLink = document.getElementById('minhasReceitas');

  sairBtn?.addEventListener('click', async () => {
    try {
      // Clear token locally; call backend logout if you have one
      localStorage.removeItem('authToken');
      msg.textContent = 'Você saiu da conta.';
      msg.className = 'mt-2 text-center text-sm text-brandBrown';
      setTimeout(() => { window.location.href = '/'; }, 700);
    } catch (err) {
      msg.textContent = 'Falha ao sair.';
      msg.className = 'mt-2 text-center text-sm text-brandRed';
    }
  });

  // Wire placeholders for future pages
  editarLink?.addEventListener('click', (e) => {
    e.preventDefault();
    window.location.href = '/usuario/editar';
  });
  minhasLink?.addEventListener('click', (e) => {
    e.preventDefault();
    window.location.href = '/usuario/receitas';
  });
</script>