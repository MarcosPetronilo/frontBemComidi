---
const title = 'BemComido - Feed';
---
<html lang="pt-BR">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>{title}</title>
    <link rel="stylesheet" href="/src/styles/global.css" />
  </head>
  <body class="min-h-screen">
    <!-- Navbar pós-login -->
    <header class="w-full bg-brandRed text-white shadow-md">
      <div class="max-w-6xl mx-auto px-4 py-3 flex items-center gap-3">
        <button class="px-3 py-1 bg-[#b21f1f] rounded shadow-button">Top Receitas</button>
        <button class="px-3 py-1 bg-[#b21f1f] rounded shadow-button">Favoritos</button>
        <button class="px-3 py-1 bg-[#b21f1f] rounded shadow-button">Buscar Receitas</button>
        <div class="mx-auto flex items-center gap-2 text-2xl font-logo">
          <span>BC</span>
          <span aria-hidden="true">☕</span>
        </div>
        <a href="/enviar-receita" class="px-3 py-1 bg-[#b21f1f] rounded shadow-button">Enviar Receita</a>
        <div class="flex items-center gap-2 ml-2 relative">
          <span>Bem vindo, Usuário</span>
          <span class="inline-block h-7 w-7 rounded-full bg-white text-brandRed grid place-items-center">👤</span>
          <button id="userMenuToggle" class="ml-2 px-2">⋮</button>
          <div id="userMenu" class="hidden absolute right-0 top-10 bg-[#f9e3ac] text-brandBrown rounded-md shadow-md ring-1 ring-black/10 w-48">
            <button id="editarInfo" class="w-full text-left px-4 py-2 hover:bg-black/10">Editar Informações</button>
            <button id="minhasReceitas" class="w-full text-left px-4 py-2 hover:bg-black/10">Minhas Receitas</button>
            <button id="sair" class="w-full text-left px-4 py-2 hover:bg-black/10 text-brandRed">Sair</button>
          </div>
        </div>
      </div>
    </header>

    <!-- Conteúdo principal -->
    <main class="max-w-4xl mx-auto px-4 py-10">
      <h1 class="text-3xl text-brandBrown italic mb-6">Coloque a mão na massa...</h1>

      <!-- Busca por ingrediente -->
      <div class="flex items-center gap-3 mb-10">
        <div class="flex-1 flex items-center gap-2 bg-white rounded-md border border-black/60 p-2 shadow-sm">
          <span class="text-brandBrown">🔍</span>
          <input type="text" placeholder="Digite o ingrediente" class="flex-1 outline-none" />
        </div>
        <button class="px-3 py-1 bg-brandRed text-white rounded shadow-button active:translate-y-[1px]">Adicionar</button>
      </div>

      <!-- Seção: Novas Delícias de seus seguidores -->
      <section>
        <h2 class="text-xl text-brandBrown mb-4">Novas Delícias de seus seguidores</h2>
        <div class="grid grid-cols-1 gap-6">
          <!-- Card de receita -->
          <article class="bg-white rounded-xl border border-black/60 shadow-sm p-4">
            <div class="flex items-center gap-2 text-sm text-brandBrown mb-2">
              <span class="inline-block h-6 w-6 rounded-full bg-brandYellow grid place-items-center">😊</span>
              <span>Usuário</span>
            </div>
            <h3 class="text-2xl text-brandBrown mb-6">Bolo de Murango HMMM</h3>
            <div class="text-4xl text-brandBrown">🎂</div>
          </article>
        </div>
      </section>
    </main>
  </body>
</html>
<script>
  const toggle = document.getElementById('userMenuToggle');
  const menu = document.getElementById('userMenu');
  const sairBtn = document.getElementById('sair');
  const editarBtn = document.getElementById('editarInfo');
  const minhasBtn = document.getElementById('minhasReceitas');

  toggle?.addEventListener('click', () => {
    menu.classList.toggle('hidden');
  });

  document.addEventListener('click', (e) => {
    if (!menu || !toggle) return;
    const target = e.target;
    if (menu.classList.contains('hidden')) return;
    if (target === menu || target === toggle || menu.contains(target)) return;
    menu.classList.add('hidden');
  });

  sairBtn?.addEventListener('click', () => {
    localStorage.removeItem('authToken');
    window.location.href = '/';
  });

  editarBtn?.addEventListener('click', () => {
    window.location.href = '/usuario/editar';
  });

  minhasBtn?.addEventListener('click', () => {
    window.location.href = '/usuario/receitas';
  });
</script>