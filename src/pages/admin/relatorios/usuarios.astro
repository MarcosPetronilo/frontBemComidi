---
const title = 'BemComido - Relatório Geral de Usuários';
// Mock metrics and users list
const metrics = {
  total: 5325,
  ativos: 4607,
  excluidos: 547,
  novosUltimos30Dias: 213,
  banidosUltimos30Dias: 12,
};
const topUsuarios = Array.from({ length: 15 }, (_, i) => ({
  id: i + 1,
  nome: `Usuário ${i + 1}`,
  receitasPostadas: Math.floor(Math.random() * 120),
  curtidasRecebidas: Math.floor(Math.random() * 1000),
  seguidores: Math.floor(Math.random() * 800),
  status: ['ATIVO', 'BANIDO', 'EXCLUIDO'][Math.floor(Math.random() * 3)],
}));
---
<html lang="pt-BR">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>{title}</title>
    <link rel="stylesheet" href="/src/styles/global.css" />
  </head>
  <body class="min-h-screen">
    <header class="w-full bg-brandRed text-white shadow-md">
      <div class="max-w-6xl mx-auto px-4 py-3 flex items-center gap-3">
        <a href="/admin/perfil" class="px-3 py-1 bg-[#b21f1f] rounded shadow-button">Voltar</a>
        <div class="mx-auto flex items-center gap-2 text-2xl font-logo">
          <span>BC</span>
          <span aria-hidden="true">☕</span>
        </div>
      </div>
    </header>

    <main class="max-w-6xl mx-auto px-4 py-8">
      <h1 class="text-2xl text-brandBrown mb-4">Relatório Geral de Usuários</h1>
      <section class="bg-[#fff7e6] rounded-md p-6 shadow-md ring-1 ring-black/10">
        <div class="grid grid-cols-2 md:grid-cols-3 gap-4 text-brandBrown">
          <div class="p-3 bg-white rounded border border-black/10">
            <p class="text-sm">Total de Contas</p>
            <p class="text-2xl font-semibold">{metrics.total}</p>
          </div>
          <div class="p-3 bg-white rounded border border-black/10">
            <p class="text-sm">Ativos</p>
            <p class="text-2xl font-semibold">{metrics.ativos}</p>
          </div>
          <div class="p-3 bg-white rounded border border-black/10">
            <p class="text-sm">Excluídos</p>
            <p class="text-2xl font-semibold">{metrics.excluidos}</p>
          </div>
          <div class="p-3 bg-white rounded border border-black/10">
            <p class="text-sm">Novos (30 dias)</p>
            <p class="text-2xl font-semibold">{metrics.novosUltimos30Dias}</p>
          </div>
          <div class="p-3 bg-white rounded border border-black/10">
            <p class="text-sm">Banidos (30 dias)</p>
            <p class="text-2xl font-semibold">{metrics.banidosUltimos30Dias}</p>
          </div>
        </div>
        <hr class="my-6 border-black/20" />
        <h2 class="text-xl text-brandBrown mb-3">Top Usuários</h2>
        <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-4">
          {topUsuarios.map(u => (
            <div class="p-4 bg-white rounded-md border border-black/10 shadow-sm">
              <div class="flex items-center gap-2 text-brandBrown mb-2">
                <span class="inline-block h-7 w-7 rounded-full bg-brandYellow grid place-items-center">👤</span>
                <strong>{u.nome}</strong>
                <span class="ml-auto text-xs px-2 py-1 rounded bg-brandRed text-white">{u.status}</span>
              </div>
              <p class="text-sm text-brandBrown">Receitas Postadas: <strong>{u.receitasPostadas}</strong></p>
              <p class="text-sm text-brandBrown">Curtidas Recebidas: <strong>{u.curtidasRecebidas}</strong></p>
              <p class="text-sm text-brandBrown">Seguidores: <strong>{u.seguidores}</strong></p>
            </div>
          ))}
        </div>
      </section>
    </main>
  </body>
</html>
